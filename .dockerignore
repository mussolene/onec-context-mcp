# Git and IDE
.git
.gitignore
.DS_Store
.cursor
.cursorignore
*.md
!README.md

# Tests and dev — not needed in image
tests/
*.pyc
__pycache__/
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.mypy_cache/

# Scripts — not needed in image
scripts/
# docs/ нужен в образе для самодокументируемого MCP (get_mcp_workflow_guide и др.), не игнорируем
*.ipynb

# Data and local state — mounted as volumes, never copy into image
data/
.env
.env.*
*.db
*.sqlite

# Docker and CI
Dockerfile*
docker-compose*.yml
.dockerignore
.github/
Makefile

# BSL bridge (separate image)
deps/

# Build artifacts
dist/
build/
*.egg-info/
*.egg
