---
description: Дополнения по отчёту о полноте MCP: пустые ответы, URI, координаты. Основной порядок вызовов — в правиле 1c-mcp-workflow.
globs: "**/*.bsl,**/Form.xml"
alwaysApply: false
---

# MCP по отчёту (только отличия и дополнения)

Основной порядок вызовов и чек-лист — в правиле **1c-mcp-workflow**. Полный отчёт и шпаргалка — в репозитории 1c_hbk_helper (docs/) или через промпт MCP **get_mcp_guides_bundle** (при использовании вне этого репозитория).

## Пустые или нерелевантные ответы 1c-help

1. `get_1c_help_index_status` — проверить индекс и версии.
2. `search_1c_help_keyword` с **точным** именем API (`Тип.Метод`).
3. По `path` из выдачи — `get_1c_help_topic(topic_path)` (параметр **topic_path**, не `path`).

## URI для lsp-bsl-bridge (единый формат)

- **Docker:** `file:///projects/<путь>` (volume `.:/projects`). Пути с кириллицей — **обязательно URL-encoding**. Примеры: `/Менеджер/` → `/%D0%9C%D0%B5%D0%BD%D0%B5%D0%B4%D0%B6%D0%B5%D1%80/`, `/МойМодуль/` → `/%D0%9C%D0%BE%D0%B9%D0%9C%D0%BE%D0%B4%D1%83%D0%BB%D1%8C/`. URI из выдачи `project_analysis` уже URL-encoded — использовать как есть.
- **Локально:** полный file URI. При пустом ответе document_diagnostics/definition — проверить URL-encoding кириллицы.
- **Координаты:** 0-based (первая строка = 0, первый символ = 0). Из `project_analysis` берётся "Recommended hover coordinate".

## Навигация и позиционные инструменты

**Основной способ:** `project_analysis` → `symbol_explore` или `get_range_content`. Инструменты `definition`, `hover`, `call_graph`, `call_hierarchy` часто возвращают пустой результат даже при корректных координатах — считать опциональными; при необходимости координаты брать из выдачи project_analysis (Recommended hover coordinate), 0-based.

## После массовых правок

`did_change_watched_files(language="bsl", changes_json=[{"uri":"file:///...", "type":2}])`.
