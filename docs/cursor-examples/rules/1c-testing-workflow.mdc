---
description: Workflow тестирования для кода 1С/BSL и Python (onec_help). YaxUnit, Vanessa (xdd/UI), pytest, BSL LS. Где искать тесты.
globs:
  - "**/*.bsl"
  - "**/tests/**"
  - "**/*.feature"
  - "**/features/**"
  - "**/BDD/**"
alwaysApply: false
---

# 1C Testing Workflow

Подробнее: `docs/reference/1c-testing-guide.md` (в репозитории onec-context-mcp). При написании/правке тестов 1С: MCP **onec-context-mcp** (примеры API, справка) и **BSL LS** (`analyze` или IDE на модулях теста/шагов).

## BSL LS (статический анализ)

- Не runtime-тесты; ловит ошибки, предупреждения, стиль. После каждой правки BSL — `java -jar … analyze` на каталог тестов или диагностики IDE.
- Цикл: исправить → снова analyze до приемлемого уровня замечаний.

## Python (onec_help)

При изменении Python в этом проекте:

```bash
PYTHONPATH=src python -m pytest tests -v --cov=src/onec_help --cov-report=term-missing --cov-fail-under=70
ruff check src tests && ruff format src tests
```

При падении покрытия ниже 70% — добавить тесты перед merge.

## Runtime-тесты 1С (репо конфигурации 1С)

Два основных фреймворка:

- **YaxUnit** (или xUnitFor1C): unit-тесты процедур/функций BSL. **Где искать:** `Tests/`, подсистемы тестов, модули `*Тест*`. При новой логике в модулях — предлагать или писать unit-тест (YaxUnit).
- **Vanessa-Automation** (xdd, UI): BDD-сценарии (Gherkin `.feature`), data-driven (xdd), UI-автоматизация, приёмочные тесты. **Где искать:** `features/`, `BDD/`, каталоги с `.feature` и шагами. При сценариях и приёмке — предлагать/править Vanessa.

Если в проекте есть `Tests/` или `features/` (или `BDD/`) — считать тесты частью workflow и искать тесты там. **CoverageBSL** — измерение покрытия BSL в 1С:Предприятие/OneScript.
